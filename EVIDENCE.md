# Evidence log

Owner: shared -- every lane appends its own evidence here; Lane 6 maintains the file structure
(`SIH26101_TEAM_ORCHESTRATION.md` section 2). Required by
`SIH26101_MASTER_CHECKLIST.md` section 3.1.

This file is the append-only record of things that were actually checked, not asserted. Never
overwrite a failed result with a later success -- add a new row instead.

## Official-source evidence

Fill in once the SPOC/portal confirmation happens. Do not fabricate a row here before the source
has actually been retrieved.

| Date retrieved | Claim | Source URL | Local hash (if downloaded) | Retrieved by |
|---|---|---|---|---|
| _pending_ | SIH 2026 team/eligibility rules | _pending SPOC/portal link_ | _pending_ | _pending_ |
| _pending_ | Official problem-statement artifact/deadline | _pending portal link_ | _pending_ | _pending_ |

## Test/build evidence

| Date | Command | Result | Notes |
|---|---|---|---|
| 2026-08-29 | `python -m pytest` (backend) | 42 passed | See `SIH26101_MASTER_CHECKLIST.md` section 8 for the full log |
| 2026-08-29 | `npm run lint` (frontend) | passed | |
| 2026-09-01 | `backend/.venv/Scripts/python.exe -m pytest -q` | 237 passed; 2 pytest-cache permission warnings | Lane 2 Packages A–N accepted; detailed command/drill trail in `LANE2_SYNC.md` |
| 2026-09-01 | PostgreSQL 16 migration/startup drills | Alembic forward/backward, legacy adoption, partial-schema rejection and stale-revision startup refusal passed | Disposable/local Compose databases; head `cf4271f204a3` |
| 2026-09-01 | Local Keycloak 26.7.2 OIDC/RBAC verification | identity verifier, binding, bootstrap and authorization negatives passed | Development issuer only; no browser route or government IdP claim |
| 2026-09-01 | PostgreSQL backup/restore concurrency and adversarial tests | concurrent backup/restore completed; exact marker/revision/table checks passed; zero matching container temp residue | Local Docker drill only; no scheduled/offsite/encrypted DR claim |
| 2026-09-01 | Full backend gate after immutable Package P and independently accepted Package Q | 272 passed; 2 pytest-cache permission warnings | Package P destructive-path review findings remain explicitly open in `LANE2_SYNC.md`; pass count is not self-approval |
| 2026-09-01 | Live PostgreSQL 4-worker concurrency drill (Codex-reproduced race, pre-fix) | `deleted_sets=[set(), set(), {'1','2','3'}, set()]`; deleted/audit sum 3; 8 of 11 expired rows abandoned; 2 young rows correctly untouched | Confirmed a real defect: unlocked candidate SELECT let 4 concurrent PostgreSQL `--apply` workers all read the same batch. Full exact evidence and root cause in `LANE2_SYNC.md`; not overwritten by the fix below |
| 2026-09-01 | Full backend gate after Package P/S (`FOR UPDATE SKIP LOCKED` fix + O-B corrections) | 339 passed; 2 pytest-cache permission warnings; 0 failures | Implemented and live-tested by Claude Code, not yet Codex-accepted; see `LANE2_SYNC.md` |
| 2026-09-01 | Live PostgreSQL 4-worker concurrency drill (Codex's exact scenario, post-fix) | 11 expired + 2 young marker rows; per-worker deletions pairwise-disjoint; union of deleted IDs = all 11 expired IDs; deleted-count sum = 11; durable audit deleted-count sum = 11; both young rows untouched; final rerun returned `0/0` with zero new audit events; unmigrated-PostgreSQL `--apply` refusal re-confirmed unaffected | Same scenario shape as the pre-fix failure above, exact opposite (correct) result. Disposable marker rows and audit events fully cleaned up after; exact evidence in `LANE2_SYNC.md` |
| 2026-09-01 | Full backend gate after independent full Lane 2 audit (Codex ran out of session credits mid-review; Claude completed it solo) | 341 passed, 0 failures; 2 warnings observed in this exact run (SQLite datetime-adapter deprecations from two regression tests) — Codex separately reported 4 warnings (adding 2 `.pytest_cache` write-contention warnings) on a concurrent run against the same commit; both counts are accurate for their own run, not a discrepancy in the code | Claude Code; fixed two real findings from this audit: `delete_subject_data()`'s reported `deleted_counts` came from a pre-delete snapshot (could under-report under a concurrent write to the same player's own rows; the actual deletion was always correct) and `BoundPrincipal.audit_actor`'s delimiter-joined encoding was not collision-free (low severity given one issuer per deployment; fixed for consistency with the same fix already applied to `identity_bootstrap.py`). Both live-verified against real PostgreSQL |
| 2026-09-01 | Live PostgreSQL `audit_events` append-only trigger drill (migration `036de46dd515`) | Normal insert succeeds; direct `UPDATE` and `DELETE` against `audit_events` both rejected by the database with `RAISE EXCEPTION`; row survives fully unmodified; owning role's `ALTER TABLE ... DISABLE TRIGGER` bypass confirmed real, then `ENABLE TRIGGER` confirmed enforcement genuinely restored (not just silently still-disabled); `downgrade()` removes the trigger cleanly, `upgrade()` restores it; SQLite migration-chain regression suite (which runs upgrade/downgrade against a real SQLite file) confirms the migration is a true no-op there | Claude Code; a second, independent external audit proposed four DB-hardening items (RLS, full audit triggers, evidence self-hashing, legacy ETL) — three rejected with technical reasoning in `LANE2_SYNC.md` (RLS/ETL have no tenant model to target; self-hashing and the "logs lost on crash" trigger justification don't provide the claimed security property), this one correctly scoped and implemented instead. **Superseded in effect (not in fact) by the Package V row below** — Codex's own cold audit found this exact trigger, as originally shipped, made the retention job unable to ever delete its only registered category; the drill result recorded here (both UPDATE and DELETE rejected) is accurate for what `036de46dd515` did at the time, it is just no longer the deployed boundary |
| 2026-09-01 | Codex cold immutable audit of Packages S/T/U at commit `1f0c576` | `pytest -q`: 341 passed, 4 warnings (2 SQLite datetime-adapter deprecations + 2 `.pytest_cache` permission warnings), 0 failures, 50.36s; `alembic current` → `036de46dd515 (head)`; `alembic check` → no new upgrade operations. Package S re-drilled in isolation (trigger disabled): `DELETED_SIZES=[2,3,3,3]`, `PAIRWISE_DISJOINT=True`, `UNION_COUNT=11`, `DELETED_SUM=11`, `AUDIT_SUM=11`, final `(0,0)` — **ACCEPTED**. Package T's counting/encoding fixes verified against the actual bulk-DELETE rowcounts and canonical JSON encoding — **ACCEPTED**. Package U's migration mechanics verified (insert succeeds, UPDATE/DELETE both rejected, row survives, downgrade/upgrade clean) but a P1 integrated defect was reproduced at the real head: inserting one 40-day-old audit event and invoking the retention job with a synthetic 30-day maximum reached the accepted Package S `DELETE` path and failed with `ProgrammingError`/`RaiseException: audit_events is append-only: DELETE is not permitted`, `ROW_REMAINS=1` after rollback — **INTEGRATION VERDICT REJECTED**, Package U's own migration mechanics are not in dispute, only its coexistence with the retention job | Codex; full audit text and the Package V handoff spec are in `LANE2_SYNC.md` (~line 2420) |
| 2026-09-02 | Live PostgreSQL Package V drill — retiring the `audit_events` DELETE rejection (migration `4631f204d4ba`), via a new committed opt-in integration contract (`backend/tests/test_core_retention_job_postgres_integration.py`) rather than a one-off script | (1) Trigger boundary: direct `UPDATE` against `audit_events` still rejected by the database (`ProgrammingError`/`RaiseException`, row unmodified); direct `DELETE` now succeeds. (2) A synthetic 30-day maximum retention policy, injected the same way `test_core_retention_job.py` already does, successfully deleted an expired `audit_events` row through `scripts/retention_job.py` — the exact call Codex reproduced failing under Package U. (3) Four concurrent PostgreSQL workers (`FOR UPDATE SKIP LOCKED`, batch size 3) against 11 expired + 2 young real `audit_events` rows: per-worker deletions pairwise-disjoint, union of deleted IDs = all 11 expired IDs, deleted-count sum = 11, durable audit-log deleted-count sum = 11 (scoped to this drill's own actor to avoid double-counting an earlier test's own audit row in the same disposable database). (4) Both young rows survived. (5) Final rerun returned `(candidate_count, deleted_count) = (0, 0)` with no new audit event. (6) `alembic downgrade 036de46dd515` on the same disposable database restored the DELETE rejection (verified: DELETE fails, row survives); `alembic upgrade head` removed it again cleanly (verified: DELETE then succeeds) — upgrade/downgrade proven clean at the real head. All 4 tests passed (6.71s); disposable database dropped cleanly afterward (verified via `\l` before and after) | Claude Code; `alembic check` against the local Compose database returned "No new upgrade operations detected" at head `4631f204d4ba`; `git diff --check` clean; full backend gate: **341 passed** with PostgreSQL stopped (the 4 new integration tests skip, proving the opt-in contract cannot break CI/sandboxes without Docker), **345 passed** with PostgreSQL running; every prior "database-enforced append-only" claim in `README.md`/`CLAUDE.md`/`CODEX.md`/`SIH26101_TEAM_ORCHESTRATION.md`/`SIH26101_MASTER_CHECKLIST.md`/`docs/contracts/data-authorization.md` corrected to name the real boundary (UPDATE-only at the database level; DELETE governed by the retention job; genuine append-only is an application-layer property) |
| 2026-09-02 | Codex immutable review of Package V at commit `847c0a8` | `pytest tests/test_core_retention_job_postgres_integration.py -q`: 4 passed in 5.87s against real PostgreSQL. Codex independently recreated the original P1 on a separate disposable database migrated through all five revisions to head: one 40-day-old event + one 5-day-old event, synthetic 30-day maximum, real `_enforce_maximum_retention_core(apply=True)` → `DELETED_COUNT=1`, `EXPIRED_REMAINS=0`, `YOUNG_REMAINS=1`, `AUDIT_DELETED_COUNT=1` — the Package U failure is closed at the real head. `alembic upgrade head` succeeded, `alembic check` returned no new operations. Full backend gate with PostgreSQL reachable: **345 passed, 4 warnings, 0 failures in 99.14s**. `pg_database` cleanup query confirmed no leaked `sih_pkgv_%`/Codex audit databases. **Verdict: Package V's production migration/retention behavior ACCEPTED outright.** Two bounded P2 findings raised, not blocking the production accept: (1) the 4-worker concurrency regression only synchronized at thread-pool submission, not at genuinely overlapping candidate selection under open locks, and a serial (non-concurrent) execution of the same assertions would also pass, so it did not itself prove the row-claim race stays closed; (2) the disposable-database fixture cleaned up only after `yield`, so a failure between `CREATE DATABASE` and `yield` (most plausibly an Alembic migration failure) would leak the database; also, several current-status doc claims (S/T/V "await Codex", PostgreSQL "rejects every UPDATE/DELETE", test counts of 339/341-only) were stale versus the corrected paragraphs elsewhere in the same files, and the ETL rejection rationale still cited "no tenant model" instead of the accepted "no real source dataset/continuity/mapping/reconciliation contract" reasoning in some files | Codex; full review text and the exact Claude Code closure assignment are in `LANE2_SYNC.md` (Package V immutable-review and Gemini-audit-disposition entries near the end of the file) |
| 2026-09-02 | Package V hardening pass closing both Codex P2 findings, live-verified against real PostgreSQL | **Finding 2 (cleanup leak) closed:** `_disposable_postgres_database()` now wraps `CREATE DATABASE`→migrate→`yield` in unconditional `try/finally`; a new deterministic regression (`test_setup_failure_between_create_and_yield_does_not_leak_database`) monkeypatches `_run_alembic` to always raise between a successful `CREATE DATABASE` and `yield`, then queries `pg_database` directly and confirms the generated database does not exist afterward. **Finding 1 (forced overlap + negative control) closed:** a test-only `_BarrierSyncSession` (Session subclass, zero production-file changes) intercepts exactly the retention job's `SELECT ... FOR UPDATE SKIP LOCKED` candidate select and blocks each of the 4 workers at a shared `threading.Barrier` while their row locks are still held, until all 4 have independently reached that point — forcing genuine overlapping candidate selection deterministically rather than by thread-scheduling luck. A new negative control (`test_negative_control_without_skip_locked_fails_the_same_contract`) runs an equivalent, deliberately unlocked candidate-select-then-delete flow (the exact pre-Package-S bug shape) under the identical forced-overlap barrier and proves it deterministically FAILS the positive contract: only the 3 oldest of 11 expired rows are ever deleted (`union == {expired_ids[0..2]}` exactly, `total_deleted == 3`, 8 rows never selected by anyone), because every worker observes the identical unlocked snapshot before any of them writes — pure PostgreSQL MVCC/row-lock re-check semantics under the forced ordering, no sleep-based timing anywhere. A separate throwaway sanity script (not committed) confirmed the SAME broken/unlocked selection logic run **serially** (no forced overlap at all) correctly deletes all 11 rows — proving the negative control's failure genuinely depends on the barrier forcing real concurrent overlap, not some unrelated flaw in the broken shape itself. The final-rerun's audit-event absence is now also explicitly queried (`SELECT count(*) ... WHERE actor = 'test:package_v_final_rerun'` → 0), not merely inferred from the returned dataclass counts. All 6 tests (2 new + the original 4) pass in 8.65s; `pg_database` confirmed clean of `sih_pkgv_%` afterward. Full backend gate: **341 passed, 6 skipped** with PostgreSQL stopped; **347 passed** with it running. `alembic current` → `4631f204d4ba (head)`; `alembic check` → no new upgrade operations; `git diff --check` clean | Claude Code; every stale current-status claim Codex flagged (S/T/V "await Codex", PostgreSQL rejecting every UPDATE/DELETE, 339/341-only counts) corrected in `README.md`, `CLAUDE.md`, `CODEX.md`, `SIH26101_TEAM_ORCHESTRATION.md`, `SIH26101_MASTER_CHECKLIST.md`; ETL rejection rationale corrected everywhere to "no identified real source dataset, continuity requirement, approved mapping/conflict policy, reconciliation contract or acceptance owner", never "no tenant model"; awaiting Codex's narrow re-review of exactly: forced-contention validity, pre-yield cleanup, final-rerun audit-event absence, current-doc consistency |

## Demo rehearsal evidence

| Date | Rehearsal # | Outcome | Adversarial judge | Notes |
|---|---|---|---|---|
| _pending_ | | | | Five consecutive offline runs required before this row can read "done" (`SIH26101_TEAM_ORCHESTRATION.md` section 5, Lane 6 acceptance evidence) |

## How to add a row

1. Run the actual command or retrieve the actual source.
2. Record the exact date, exact result (not a paraphrase), and who did it.
3. If it failed, record the failure -- do not wait for a passing run to add the row.
