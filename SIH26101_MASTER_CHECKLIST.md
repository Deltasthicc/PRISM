# SIH26101 master checklist

Last evidence review: 3 September 2026

Historical repository baseline: commit `429df46` on `main`

Current Lane 2 evidence base is integrated on `main` through Package W-C commit `8d0d1de`.
Packages A–V are accepted as recorded in `docs/internal/LANE2_SYNC.md`; W-A is independently accepted, Claude's
W-B handbook/operator work passed Codex review after two real schema-drift defects were found, and
the final narrow W-C legacy-column count repair awaits Claude's immutable review.
Package P/S (retention enforcement, including atomic PostgreSQL row-claiming for concurrent
`--apply`) and Package T (a full independent security/data audit) are **ACCEPTED by Codex** on
independent cold immutable review, including Codex's own reproduction of the four-worker
concurrency drill and the data-rights/audit-actor fixes. Package U's original database trigger
(rejecting both `UPDATE` and `DELETE` on `audit_events`) was found by that same Codex review to
conflict with the accepted retention job -- Package V's follow-up migration (retiring only the
`DELETE` rejection) fixed this, and Codex's review of Package V **accepted its production
migration/retention behavior outright**, raising two bounded test/evidence-hardening findings (a
disposable-database cleanup gap and a concurrency test needing genuinely forced overlap plus a
negative control) that are closed and independently accepted after Codex's narrow immutable
re-review of `ac5a2e7`. See `docs/internal/LANE2_SYNC.md` for the full audit trail.

Requirement source: `docs/SIH26101_PROBLEM_STATEMENT.md` (`PS-01`…`PS-18`)

Companion documents: `docs/internal/SIH26101_WINNING_PLAYBOOK.md` and `docs/internal/SIH26101_TEAM_ORCHESTRATION.md`.
`docs/internal/LANE2_INTEGRATION_GUIDE.md` is the current per-lane database/repository usage and two-way handoff
contract. `docs/internal/LANE2_HANDOFF_FOR_OTHER_LANES.md` catalogs dated issues raised after live testing.

This is the single execution ledger for the project. It separates what the repository proves, what an official source proves, and what is still a proposal. A checked item means its acceptance evidence exists; it does not mean that an AI suggested it.

## Evidence labels

- **VERIFIED** — inspected or executed against this repository.
- **OFFICIAL** — supported by a primary government/standards source.
- **DECISION** — a deliberate team choice, not an external fact.
- **PROPOSED** — useful design direction that still needs implementation and validation.
- **BLOCKED-EXTERNAL** — cannot be truthfully completed without a partner, official approval, or real pilot data.

## 1. Current truth, without pitch inflation

| Area | Current state | Evidence |
|---|---|---|
| Backend | FastAPI/SQLAlchemy; SQLite zero-setup demo plus PostgreSQL 16/Alembic profile with migration-gated PostgreSQL startup | **VERIFIED** |
| SQLite dev-mode schema drift | **FOUND & FIXED 9 September 2026:** any local `app.db` created before the `full_name` column was added to `learner_profiles` (this session) 500'd on every profile/pathway read with "no such column: learner_profiles.full_name" -- `Base.metadata.create_all()` never alters an existing table, and `main.py`'s `ensure_columns()` SQLite dev-mode patch list (the established mechanism for exactly this) was never updated for that column. Fixed and verified against a real reproduction of the broken database. Any future column added to an existing SQLite-backed model needs the matching `ensure_columns()` call, not just the Alembic migration -- both are required, one is not a substitute for the other. See PR #39. | **VERIFIED** |
| Automated tests | Package W-C full gates on 3 Sep 2026: **442 passed, 6 skipped** with PostgreSQL stopped; **448 passed** with local Compose PostgreSQL healthy. Live status reported Alembic `640603a37f2f (head)`, all 17 allowlisted tables present, and no model/schema drift. Earlier counts remain historical, not overwritten; remote CI is still not claimed | **VERIFIED** |
| Curricula | Four curricula and 55 competencies are seeded and usable through backend APIs (this row previously read "34 competencies" -- **CORRECTED 10 September 2026**: stale for some time, `services/curricula.py` has carried 55 for a while and nothing had re-counted it). All 33 named competencies in `docs/SIH26101_PROBLEM_STATEMENT.md`'s explicit scope table map to a real competency_id (`services/ps02_coverage.py`, enforced at import). | **VERIFIED** |
| Quest UI | **UPDATED 9 September 2026:** `/dungeon` ("Prerequisite Pathways") was rebuilt to be real and curriculum-aware for all four domains, not DSA-only -- driven by `GET /learning/pathway/{player_id}` (the same engine `/stats` uses) merged with real per-player room unlock/accuracy status from a newly player-aware `GET /game/dungeon/{id}`. The deleted `frontend/app/combat/[roomId]` per-topic combat page was restored. Along the way, a real, previously-unfound authorization bug was fixed: every handler using a bare `scoped_to_own_player(...)` check (dungeon session start, room entry, answer submission, guild raids, hint/powerup use) had no demo-mode bypass, so it 403'd unconditionally under `DISABLE_AUTH=true` -- the app's only supported login flow. Live-tested end-to-end: a non-DSA (Official Statistics) learner solved a real competency-quiz question, fought a real combat round in `/dungeon`, and the room flipped to "mastered" and unlocked its dependents. See PR #36. **UPDATED 10 September 2026:** that 9 September pass rebuilt `/dungeon`'s own shell onto the professional (`Panel`/`Badge`/`Button`) styling but left its "Practice this competency" and capstone buttons still routing into `/combat`/`/boss` -- since Quest Mode defaults off, that route's own `QuestModeGate` then dead-ended into "turn on Quest Mode" with no plain quiz reachable at all. Fixed: `routes/competency_quiz.py`'s question-fetch endpoint gained an optional `competency_id` param (scoping the question pool to one competency instead of a whole bundled topic), and a new plain `/practice` page (same professional shell, no combat framing) is what those buttons link to now; the capstone links to the existing `/baseline-assessment` pending a curriculum-scoped capstone as a real follow-up. `/combat`/`/boss`/`/character`/`/leaderboard` are untouched, still reachable only via the opt-in Quest Mode toggle. See PR #43. **FOUND & FIXED 10 September 2026:** PR #43's `/practice` page correctly submitted to `routes/competency_quiz.py`, but that route only ever wrote `EvidenceRecord` (the competency-vector scoring table) -- `routes/game.py`'s room unlock/status/`next_topic` logic reads *only* `AccuracyHistory`, so no course's Prerequisite Pathways map could progress after finishing a quiz at all, for any learner. Fixed by having `submit_quiz` also update `AccuracyHistory` per graded answer, reusing the exact update pattern `dsa_sandbox.py`'s submit already used. Live-verified end-to-end on Official Statistics: practicing "Statistical Foundations" to 5/5 flipped it to mastered and unlocked four previously-locked downstream rooms. See PR #45. | **VERIFIED** |
| Default landing / professional theme | Root `/` links authenticated learners to `/academy`, and successful login/registration now route there; `Providers` only bootstraps auth and does not navigate. Quest is no longer the startup route, and (6 Sep 2026) is now gated behind an explicit `preferred_mode` toggle rather than just convention -- see the Quest-mode decision above. `AcademyHub.jsx` no longer imports the `Pixel*` kit (it was already gone by 6 Sep 2026, this claim was stale). The dead, unused `torch-flicker` div (no matching CSS rule) was removed from the global layout the same day. **UPDATED 9 September 2026:** found and fixed two more places the professional theme had NOT actually been carried through despite being on the always-visible path: `/dungeon` ("Prerequisite Pathways", never gated behind Quest Mode) was still built entirely from the `Pixel*`/dungeon-skin kit (monster sprites, dark backgrounds, RPG flavor text), as was `/admin` (an aggregate metrics dashboard with no connection to Quest Mode at all), and the app-wide onboarding tour shown to every learner was 6 of its 7 slides about villains/heroes/boss fights. All three rebuilt on the professional shell (`Panel`/`Badge`/`Button`, same as `/stats`/`/academy`). Quest Mode's own opt-in combat screens (`/combat`, `/boss`, `/character`, `/leaderboard`) were deliberately left with their existing skin -- that's the separate, already-documented Quest-mode decision above, not reversed. See PR #39. Also added a "Competency & Gap Analysis" nav tab to `/stats` (previously reachable only via the avatar chip, a real contributor to routing confusion -- a tester had guessed `/competency-and-gap-analysis` and `/prerequisite-pathways` as literal URLs and 404'd both). The global layout still loads pixel fonts and mounts the (muted-by-default, no on-switch anywhere in the UI) music player unconditionally -- still open, not addressed by this pass | **VERIFIED** |
| Register/login parity | **FOUND & FIXED 9 September 2026:** `/register` rendered with the full authenticated app chrome (navbar, Quest Mode toggle, etc.) around the plain signup card whenever a stale session existed in `localStorage`, while `/login` correctly stayed chrome-free in the same situation -- `MainShell.jsx` only special-cased `/login`. Both are now treated as the same pre-authentication entry point. See PR #39. | **VERIFIED** |
| Competency targeting | **UPDATED 10 September 2026:** designation/job role/department/current_assignment now genuinely change a learner's target level through the real HTTP routes -- previously true only in isolation (`services/role_targets.py`'s in-memory `ROLE_TARGET_OVERRIDES` and `services/role_target_resolver.py`'s DB-backed precedence lookup were both fully correct and already wired into `routes/learning_competency.py`), but the `role_targets` DB table itself was never seeded with any row, so every real request fell through to `curriculum-default` regardless of profile fields -- the resolver had nothing to find. Fixed with `db/seed.py::seed_role_targets()`, deriving real DB rows directly from `ROLE_TARGET_OVERRIDES` (not a second hand-typed table, so the in-memory and DB-backed paths can't drift apart) and wired into `main.py`'s existing `SEED_DEMO_DATA` gate. Live-verified end-to-end: a learner profile with `job_role="Statistical Officer"` now resolves `os_statistical_foundations` to target 4 with `source="internal-prototype"`/`matched_field="job_role"` through the real `resolve_role_targets()` call path, while a competency with no override for that role still correctly falls back to `curriculum-default`. Qualifications and career goal remain stored-but-unused; no reviewer has approved any of these prototype numbers as an official MoSPI/CBC target. | **VERIFIED** |
| Assessment formula | 65% demonstrated performance + 35% self-rating when both exist; this is a transparent prototype policy, not validated psychometrics | **VERIFIED** |
| Quiz grounding | TXT/MD/PDF/DOCX extraction is bounded; generated excerpts are matched after whitespace/case normalization, not as literal byte-for-byte substrings. **UPDATED 9 September 2026:** the nav-labeled "Source Quiz Generator" (`/quiz`) was itself a fully hardcoded mockup with zero backend calls, disconnected from this real, already-working `POST /learning/quiz/generate` feature (only reachable before via `/academy`). Rebuilt `/quiz` as a real dedicated page sharing a `QuizGeneratorPanel` component with `/academy`; wired up the previously-dead `listQuizzes` API into a real quiz-history view. Also found and partially closed two content gaps against the real 49-document corpus: `os_agricultural_statistics` had zero questions and was mapped into no quiz topic at all (fixed, 2 real source-cited questions added); `os_statistical_programming`'s claimed Stata/SPSS/SAS coverage only ever tested Python/R (added one real SPSS question; Stata/SAS genuinely have no source anywhere in the corpus and are left honestly uncovered rather than fabricated). See PR #38. **EXPANDED 10 September 2026:** generated quizzes had no per-question difficulty and no way to actually take/score one (`QuizPreview` only ever revealed every answer immediately). Added a per-question easy/medium/hard tag to both the Gemini prompt/schema and the extractive fallback (a documented, explainable length/clause-complexity heuristic where no model is available to judge it), plus a real `POST /learning/quiz/{quiz_id}/submit` endpoint and interactive "Take quiz (scored)" mode using the same time+difficulty-weighted algorithm `competency_quiz.py` already used (factored into a shared `services/quiz_scoring.py` so there is one algorithm, not two) -- deliberately kept out of `AccuracyHistory`/the real competency vector since a generated question's `competency` field is free text, not a real `competency_id`. Also added a "Fixed question bank" mode to `/quiz` surfacing the existing 22-topic competency-quiz bank as a no-upload alternative. Live-tested end-to-end: uploaded a real hand-crafted PDF, confirmed `pypdf` extraction and Gemini-grounded generation produced accurate source-cited questions with real difficulty tags, then scored a real attempt (2/5 correct, 38.5% weighted) and confirmed the history list's "best score" updates and keeps the higher of two attempts. See PR #40. **EXPANDED 10 September 2026 (hand-authored competency-quiz bank, separate from the generated-quiz path above):** `os_survey_design` was the one PS-02-named competency with real, hash-verified, tagged source material (`plfs_2017_18`, `expert_group_cpi` -- 901 combined pages) and zero questions -- fixed with 5 real questions transcribed from PLFS 2017-18's actual "Sample Design of PLFS" chapter (rotational panel design, PPSWR FSU selection, SRSWOR second-stage selection, real numeric FSU allocations), and the competency added to the `statistical_foundations` quiz topic (it had been in no topic at all). Also fixed a real manifest/content mismatch: `os_agricultural_statistics` already had 2 real questions sourced from `sdg_nif_2026`, but that document's `competency_ids` tag in `data/document_corpus.json` never listed `os_agricultural_statistics` -- `documents_for_competency()` returned an empty list for a competency the bank actually had content for. Beyond that, mined 10 more real, hash-verified corpus documents that were previously cited by zero questions (`cpi_imputation`, `sif_guideline`, `nas_state_district`, `cca_dsc_interop`, `certin_govt_entities`, `cbc_kqf`, `manual_asi_2008`, `national_geospatial_policy`, `nqaf_cocsso`, `niti_ai_strategy`) for 21 more real questions, prioritizing `os_ml` (the weakest Technical-category competency at 1 question) and several other thin competencies (`os_data_quality`, `os_metadata_standards`, `os_price_statistics`, `os_industrial_statistics`, `os_gis`, `os_national_accounts`, `dl_cyber_hygiene`, `dl_digital_signatures`, `pa_leadership`). Bank grew from 148 to 174 real items across 46 cited corpus documents (up from 36). Stata and SAS still have zero source material anywhere in the corpus and remain honestly uncovered, per the existing PR #38 finding -- no fabrication there. All new doc_id citations added to `tests/test_competency_hand_authored.py`'s `RELIABILITY_DOC_IDS` (an exact-match set, so a new citation fails loudly if missed); one existing test's fixed "known doc with zero items" example (`sif_guideline`) was swapped to `cpi_faq_2024` since `sif_guideline` now has real content. **EXPANDED 11 September 2026:** the one remaining zero-question DSA competency, `binary_search` (the array algorithm -- distinct from `binary_search_tree`, which already had 4 questions), closed. Checked all 6 currently-live GATE CS1/CS2 papers already used elsewhere in this bank (2024, 2025, 2026) for genuine array binary-search content first -- every "binary search" mention in those turned out to be about BSTs, already cited. Found real coverage instead in GATE's newer Data Science & AI (DA) paper: downloaded and hash-verified GATE 2025 DA and GATE 2026 DA directly from their official IIT Roorkee/IIT Guwahati hosts (sha256 + byte count confirmed against the live download, not assumed), added both as new `document_corpus.json` entries (51 total, up from 49), and transcribed 2 real questions -- GATE 2025 DA Q.27 (why binary search needs both a sorted *array* and O(1) random access, so a sorted linked list still doesn't qualify) and GATE 2026 DA Q.31 (worst-case comparison count for an unsuccessful search on 1000 elements; the "10" answer was independently confirmed by simulating binary search over every not-found target rather than trusted from a secondary source). `binary_search` added to the `linear_structures` quiz topic, which previously excluded it by name in a code comment. Bank grew from 174 to 176. | **VERIFIED** |
| Assistant/media scope | No learner virtual assistant; no PPTX or video/audio transcript ingestion | **VERIFIED** |
| Retrieval | **CORRECTED 10 September 2026:** this row was stale -- a real, working retrieval pipeline exists and is wired into `/ai/*`: `ai/ingestion.py::_split_into_chunks()` (sentence-boundary chunking with page/slide/section/timecode provenance) feeds `ai/retrieval.py`'s `InMemoryChunkStore`, which does pre-ranking tenant/role access filtering, real BM25 scoring (smoothed IDF, TF saturation, term-overlap, a verbatim-bigram phrase boost) and abstains below a documented relevance threshold, with `create_citations_from_retrieved_chunks()` producing real page/locator citations -- `ai/assistant.py` uses this for the grounded-answer path. `tests/test_content_ai_rag.py` (cross-tenant isolation, role filtering, ranking, abstention, citation resolution) already covers it. What is still missing is only semantic embeddings/a vector store (ranking today is lexical BM25, not embedding similarity) -- and the user-facing `POST /learning/quiz/generate` path (`services/quiz_generator.py`) does not use this retrieval engine at all: `services/content_ingestion.py::extract_text()` computes chunks via `ingest_document()` and then discards them, passing the whole flattened text to a single Gemini call (or the extractive fallback) instead of routing through `default_chunk_store`. That specific wiring gap remains open. | **VERIFIED** |
| Recommendations | Internal practice plus links to provider catalogues; no real enrolment, completion, or catalogue sync. **UPDATED 10 September 2026:** `GET /learning/integrations/status` previously reported `mode: "configured"` from nothing but `bool(os.getenv("IGOT_API_BASE_URL"))` -- setting that variable to any string, even a typo or an unreachable host, flipped the UI to "Live iGOT Karmayogi API configured" with zero verification, violating `docs/contracts/provider-adapter.md`'s own stated rule ("an environment variable alone must never imply LIVE"). Fixed: `integrations/provider.py` gained `LiveHTTPProviderAdapter`, which makes a real short-timeout HTTP health check against `{base_url}/health` and reports `LIVE` only on a genuine 2xx, `ERROR` otherwise; `services/learning_catalog.py::integration_status()` now reports the fixed, documented `LIVE`/`CATALOGUE` vocabulary (not the old ad hoc `"configured"`/`"catalog-fallback"` pair) based on that real check, not the variable's presence. `SimulatedIGOTAdapter` (previously imported only by `tests/test_api_integration_lane5.py`'s contract tests, otherwise fully unwired) is unaffected and remains the honest default path. No real iGOT/NSSTA endpoint, credentials or sandbox exist for this project (PS-05/PS-17, tracked BLOCKED-EXTERNAL in section 4.3 below), so pointing `IGOT_API_BASE_URL`/`NSSTA_API_BASE_URL` at anything today will genuinely, correctly report `CATALOGUE` with an "unreachable" detail -- that is the intended honest result of "real mode" against a boundary this project cannot cross alone, not a bug. | **VERIFIED** |
| iGOT boundary | Authenticated iGOT/KB-iGOT interfaces exist publicly in integration documentation, but this project has no approved endpoint contract, credentials, or sandbox | **OFFICIAL + VERIFIED** |
| Identity/security | Product routes remain username/player-ID demo interfaces and are not protected. Lane 2 implements local OIDC JWT verification, issuer/sub binding, fixed RBAC/bootstrap, deployment-database tenant guards and audited security/data-rights primitives; browser SSO, row-level organization tenancy, route enforcement, approved production IdP, rate limits and secrets operations remain open | **VERIFIED** |
| Admin metrics | Aggregate-only response, but repeated historical assessments inflate `learner_count` for a gap | **VERIFIED** |
| Dashboard coverage | No provider-derived learning hours, training-effectiveness measurement, emerging-skill analysis or validated predictive analytics | **VERIFIED** |
| Learner Assistant (RAG) | **WIRED UP 10 September 2026:** the retrieval/assistant engine (`ai/retrieval.py`, `ai/assistant.py`, `/ai/assistant/query`) was real, tested (`test_content_ai_assistant_eval.py`) and completely orphaned from the frontend, and its in-memory chunk store started empty regardless (nothing indexes it except a manual `/ai/retrieval/index` POST). Fixed both: `ai/seed_corpus.py` indexes all 174 hand-authored questions' `source_excerpt` fields at app startup (real, committed, human-reviewed text -- no network fetch, so it reproduces on a fresh clone unlike the gitignored PDF doc cache), and a new `/assistant` page (added to nav, all strings translated into all 11 languages) calls the real endpoint. **FOUND & FIXED during that work:** seeded chunks need `tenant_id=DEPLOYMENT_TENANT_SCOPE`, not the generic `"default"` placeholder `Chunk`/`AccessContext` default to -- every real principal this app issues (the `DISABLE_AUTH` demo principal included) carries that tenant scope, so seeding under the wrong constant would have made the assistant abstain on every real query in the actual running app despite the store being fully populated; caught only by an end-to-end test through the real route, not the isolated unit tests. Also exposed and fixed a latent, pre-existing test-isolation bug in `test_content_ai_auth_security.py`'s cross-tenant test (it patched a module-level name the `default_assistant` singleton's already-bound `chunk_store` attribute never actually read). Backend 967 passed; live-verified a real cited answer, an honest abstention on a genuinely out-of-domain query, and correct rendering in Tamil. See PR #46. **FOUND & FIXED 11 September 2026:** live-tested on the real deployed `/assistant` page with an invalid `GEMINI_API_KEY` (extractive-fallback mode), asking "who are you" returned an unrelated GATE 2024 quiz question labeled as a grounded answer instead of abstaining. Root cause in `ai/retrieval.py`'s `InMemoryChunkStore.search()`: a query made entirely of stop words ("who", "are", "you") fell back to scoring against those raw tokens instead of abstaining, and since common words appear in nearly every chunk, `overlap_ratio` alone pushed the relevance score past the 0.20 abstention threshold regardless of real relevance. Fixed by abstaining immediately when a query has zero content (non-stop-word) tokens, matching the same "no signal, no answer" principle `services/course_catalogue.py`'s unrelated search function already followed (`test_search_on_stopwords_alone_returns_nothing`). New regression test added. See PR #54. | **VERIFIED** |
| Voice assistant UI | **WIRED UP 10 September 2026:** the voice pipeline (`ai/voice/*`, `routes/ai_voice.py`'s authenticated WebSocket `/ai/voice/stream`, 61 test functions) was real and tested but had zero frontend presence. **FOUND & FIXED:** `_authenticate_token()` had no `DISABLE_AUTH` bypass, unlike every other route -- it always required a genuine Keycloak-issued bearer token, meaning voice could never connect in exactly the `DISABLE_AUTH=true`/no-Keycloak local setup this app is normally run in. Fixed by reusing `routes/authorization.py`'s existing `_demo_principal()` bypass here too (same trust boundary, same "never for real user data" warning), with regression tests confirming both the unchanged real-auth path and the new bypass. New `/voice` page (added to nav, all strings in all 11 languages): captures mic audio, resamples to 16kHz mono PCM16 client-side (Web Audio API), streams it over the WebSocket, and shows the live transcript plus the same grounded/cited answer `/assistant` shows; handles `TTS_FAILURE` (no local Piper model, a pre-existing documented constraint) with an honest "voice reply unavailable" note rather than silence, and handles barge-in and a denied microphone. **Verification note:** the actual WebSocket protocol was verified end-to-end with a real generated speech sample (Windows SAPI TTS -> WAV -> 16kHz PCM16) fed through `TestClient`'s ASGI transport -- real VAD, real `faster-whisper` transcript, real RAG-grounded answer with citations, honest `TTS_FAILURE`. Live microphone capture through an actual browser was **not** verified (no physical microphone in this environment) -- flagged explicitly rather than claimed. Backend 969 passed; frontend build clean. See PR #47. | **VERIFIED** |
| Whisper domain fine-tuning | **TRAINED AND MEASURED 10-11 September 2026:** asked whether any real ML-training opportunity existed in the project. Investigated `/ai/difficulty/next`'s "RL epsilon-greedy bandit" and `/ai/graph/next-topic`'s "Knowledge Graph AI" first and ruled both out -- reading `services/ai_client.py`/`routes/ai_real.py` shows both are fixed-threshold heuristics with no persisted learned state or training data at any scale, so training either would just be ML-washing a heuristic that already works; left untouched per explicit instruction to leave anything without a genuine trained-model use case. Found one real candidate instead: the voice pipeline's `faster-whisper tiny.en` STT measurably mis-transcribes this project's own domain vocabulary. Confirmed twice, independently, with two different TTS voices on clean synthesized speech (no microphone/recording-artifact excuse available): Windows SAPI's "DARPG Sevottam" transcribed back as "DARPAGE SEV-autumn"; a separately downloaded Piper `en_US-lessac-medium` voice's "The DARPG Sevottam framework..." came back "The dark, sevetam framework...". Built the full fine-tuning pipeline in `backend/scripts/voice_finetuning/`: a data-prep script (1089 deduplicated training sentences from `services/curricula.py` and all 174 hand-authored questions, committed as `corpus.txt`), a multi-voice Piper TTS synthesis script (4 free voices for speaker diversity), a standard HuggingFace `transformers`/`datasets`/`Seq2SeqTrainer` fine-tuning script, a baseline-vs-fine-tuned WER evaluation script, and a runbook with the exact commands for a GPU server. Added a `WHISPER_MODEL_PATH` env-var override to `ai/voice/stt.py` (mirroring `LocalPiperTTS`'s `PIPER_MODEL_PATH`) so a fine-tuned model wires in with zero further code changes, with regression tests for the default/env-var/explicit-arg precedence. **Then actually run on the user's own RTX 5090 server, twice independently (reproducible result):** `openai/whisper-tiny.en` fine-tuned for 8 epochs on the 1002-example synthetic corpus, converted to CTranslate2, and evaluated against the stock baseline on 87 held-out validation examples -- **36.35-36.68% WER (baseline) -> 28.28-28.82% WER (fine-tuned), a 21-22% relative improvement**, both runs agreeing within noise. Sample mismatches show the fine-tuned model correctly recovering `MoSPI`, `I.T. Act`, and `NIF` where the baseline mangled them. Found and fixed four further real bugs surfaced only by actually running this on real hardware (each shipped as its own PR, not just patched locally): `datasets.map()`'s `num_proc=1` spawning a multiprocessing worker pool that hung indefinitely on a real GPU box (PR #50, root-caused via a `KeyboardInterrupt` traceback into `conn.recv()`); `transformers`' `Trainer`/`Seq2SeqTrainer` removing the `tokenizer=` kwarg in favor of `processing_class=` in newer releases (also PR #50); `whisper-tiny.en`'s generation config lacking any `lang_to_id` mapping, crashing `generate()` when an explicit language/task was forced on an English-only checkpoint (also PR #50); and `datasets==5.0.1`'s `Audio` feature requiring `torchcodec` to decode with no fallback, which then crashed with a native ABI mismatch against the pinned torch build -- fixed by dropping `datasets.Audio` entirely in favor of direct `soundfile`/`librosa` decoding, and correcting the runbook's torch install command, which had silently resolved a build with no support for the RTX 5090's sm_120 (Blackwell) architecture despite `torch.cuda.is_available()` printing `True` (PR #51). The trained model was then copied back to a local dev machine via `scp` and verified running the actual FastAPI backend (`uvicorn main:app`) with `WHISPER_MODEL_PATH` pointed at it. Model file itself is not committed (a machine-specific build artifact, same as the Piper voice model) -- anyone can reproduce it from the documented runbook. | **VERIFIED** |
| Guild/leaderboard | Both now gated behind the opt-in `preferred_mode` Quest toggle (6 Sep 2026), off the professional path by default. Leaderboard heading corrected to "ALL-TIME RANKS", matching the real lifetime-XP ranking -- no weekly window exists. **UPDATED 9 September 2026:** the `/guild` route's old content (a self-contained fake WASM code-arena mockup, `AdaptivePracticeDsaQuest`) was deleted entirely and that URL repurposed to a new, genuinely real feature: `/dsa-sandbox` (real Judge0 code execution, an 11-problem original bank, gated to learners who selected DSA Fundamentals). The unrelated multiplayer guild-raid backend (`/game/guild/*`, real and working) is untouched by this -- `joinGuildRaid()` still has zero frontend callers and remains open. See PR #37. **EXPANDED 10 September 2026:** the sandbox judged Python only; confirmed live against Judge0's `/languages` API that C++, C#, Java and JavaScript were also available, then designed a shared restricted-JSON wire protocol and a hand-rolled minimal JSON parser per statically-typed language (Judge0 has none built in for Java/C++/C#) so every problem's starter code, driver harness and test cases are generated once from small per-problem type metadata (`services/dsa_lang_gen.py`), not hand-duplicated five times. All 14 problems (11 original + 3 new: Move Zeroes, Detect a Cycle, Longest Increasing Subsequence) verified Accepted against the real Judge0 API in Java/C++/C#/JavaScript (56/56 combinations), plus a Python-path spot-check of the 3 new problems and the one existing array-returning problem, before shipping; live-tested end-to-end in the running app (a real Java two-sum submission returned "Accepted -- 3/3 passed" from Judge0). **FOUND & FIXED during that verification:** the live Python judging path (`services/dsa_lang_gen.py`'s `PY_HARNESS`, not the old unused `services/dsa_problems.py` constant of the same name it replaced) still serialized results with `json.dumps`'s default spaced separators (`"[0, 1]"`) while every other language's hand-rolled stringifier -- and the test cases' own `expected_output` -- had just been made compact (`"[0,1]"`) for cross-language consistency; any real Python submission returning a list would have silently graded as Wrong Answer. Caught by the same live-Judge0 spot-check, not assumed correct from code review. See PR #40. **OVERHAULED 10 September 2026 (Codex, integrated via PR #44):** replaced the plain textarea editor with a real CodeMirror 6 workspace (syntax highlighting per language, line numbers, bracket matching, autocomplete, folding, four-space indentation, Ctrl/Cmd+Enter to run, Reset), addressing a real reported indentation bug in the old textarea. Expanded the bank from 14 to 22 original problems (8 new: sliding window, hash maps, two pointers, backtracking, intervals, matrices, prefix sums, connected components), each with two worked examples, constraints and a collapsible solution outline. Added a searchable/filterable problem browser. Backend: 961 passed; frontend build clean; live-verified with a real Python two-sum submission against Judge0 (Accepted, 3/3). The merge into `main` also had to machine-translate 10 new UI strings this branch added (it forked before the 9-language i18n expansion) into the other 9 languages so `dsaSandboxPage` stayed complete across all 11. | **VERIFIED** |
| Delivery | CI workflow and Alembic migration framework exist; no fresh remote-CI result is claimed here. Frontend tests, production deployment definition, observability stack and repository licence remain absent/unverified | **VERIFIED** |

A clean local frontend production build completed independently on 3 September 2026 (14 routes),
but this is not remote-CI or deployment evidence. A clean CI build remains mandatory.
Remote CI (`backend-tests`, `frontend-checks`, `contract-checks`, `sast`, `security-checks`) has
since passed on GitHub Actions for each of PRs #35-#38 (9 September 2026), each merged to `main`;
the local frontend build was 17 routes as of PR #38.

## 2. Decisions and corrections from the pasted 174 KB planning source

- [x] ~~**Keep Quest mode as an optional engagement layer.**~~ **SUPERSEDED 6 September 2026, then RE-IMPLEMENTED same day as an explicit opt-in toggle:** `/stats` and `/guild` were rebuilt on the professional shell (real data, no `Pixel*`/dark-cave styling) as the first step. The remaining RPG-skinned routes (`/dungeon`, `/boss/[dungeonId]`, `/character`, `/guild`) are now gated behind `players.preferred_mode` (`backend/models/enums.py`'s `LearningMode`, migration `640603a37f2f`, previously a storage-only foundation with no route or frontend consumer -- see `backend/tests/test_core_learning_mode.py`'s own docstring at the time). `POST /game/player/{player_id}/mode` (new) reads/writes it; `GET /game/player/{player_id}` now actually returns it (`_serialize_player()` had never included the field despite `schemas.player.PlayerResponse` declaring it -- a real bug, fixed alongside this). The NavBar exposes a "Quest Mode" toggle, off (`professional`) by default; the four Quest routes render `components/QuestModeGate.jsx` instead of their content when off, rather than silently redirecting. `frontend/components/AcademyHub.jsx`'s "Start Quest" links (previously pointing at a non-existent `/dungeon/{dungeon_id}` dynamic route) and `frontend/app/quiz/page.jsx`'s "Practice in DSA Quest" link now point at the real static `/dungeon`/`/guild` routes and only appear once Quest mode is on. Original text, kept for history: "Do not execute the pasted recommendation to delete it. The primary experience must become a professional learning workspace; Quest mode is a secondary practice mode... Quest's pixel-art/dungeon skin visually scoped to opt-in Quest routes (`/dungeon`, `/combat`, `/boss`, `/guild`, `/character`) only." **DECISION (RESTORED, now actually enforced by a toggle rather than by convention)**
- [x] **Do not call the proposed labels “official FRAC levels.”** FRAC officially connects roles, activities and competencies. CBC now publishes the Karmayogi Competency Model (KCM) and says it is integrated with iGOT. The labels “Basic Awareness” through “National Expert” were not verified in the cited official material. **OFFICIAL**
- [x] **Treat Role Readiness Index as an internal product metric only.** Any formula and weighting must display its version, inputs and provisional status until an authorized domain owner validates it. **DECISION**
- [x] **Do not invent iGOT course IDs, URLs, completion records, NSSTA schedules, API success, or government approval.** A simulator must be visibly labelled `SIMULATED`; a live adapter must prove authenticated partner connectivity. **DECISION**
- [x] **Reject the baseline claim “JWT already exists.”** It did not exist at `429df46`; Package I
  subsequently added and live-tested local OIDC JWT verification. **VERIFIED**
- [x] **Reject “all four domains work end-to-end.”** Only backend coverage is cross-domain today; browser Quest routing is DSA-only. **VERIFIED**
- [x] **Reject the pasted 28/100 score and fixed judging weights as official.** They may be private prioritization aids only. **DECISION**
- [x] **Keep the useful additions:** item-review lifecycle, grounded RAG evaluation, bilingual path, one statistics lab, adapter/simulator boundary, privacy/security gates, and contract-first team ownership. **DECISION**

### 2.1 Requirement traceability from the new problem-statement capture

- [x] Preserve the complete supplied scope as numbered requirements `PS-01`…`PS-18` in `docs/SIH26101_PROBLEM_STATEMENT.md`.
- [x] Map every requirement to one primary lane and distinguish a synthetic demo from controlled-pilot/production evidence.
- [ ] Require every implementation issue and PR to name at least one `PS-*` requirement or explicitly state `infrastructure-only`/`defect-only`.
- [ ] Maintain a final evidence matrix with columns: requirement, implementation, test/demo proof, truth status, owner and remaining external gate.

## 3. P0 — eligibility, truth and an unbreakable vertical slice

Do these before adding new “AI” features.

### 3.1 Eligibility and source freeze

- [ ] Obtain the current SIH 2026 rule/guideline PDF or portal export from the college SPOC and store its link/hash in the evidence log.
- [ ] Confirm the six-person roster against the current official rule: all six registered students, same-college status, leader designation and required gender composition. The latest official senior-edition guideline independently retrieved is SIH 2024; the college SPOC must still confirm the 2026 rule.
- [x] Divide implementation into six parallel, disjoint ownership lanes with one controlled coding agent/task per lane; see `docs/internal/SIH26101_TEAM_ORCHESTRATION.md`.
- [ ] Confirm problem statement text, organization, category, submission deadline, deck template and idea cap from the official portal. Third-party SIH aggregators are discovery aids only.
- [ ] Assign one SPOC liaison and one final presenter; record backups for both.
- [ ] Add an `EVIDENCE.md` file containing official URLs, retrieval date, local hashes for downloaded rules, test commands and demo proof.
- [ ] Resolve repository licence/provenance before public release or transfer.

### 3.2 Repair the current product path

- [x] ~~Add a real dynamic dungeon route such as `frontend/app/dungeon/[dungeonId]/page.jsx`.~~ **RESOLVED DIFFERENTLY, 9 September 2026:** rather than a `[dungeonId]` dynamic route, `/dungeon` itself became curriculum-aware -- a dropdown selects among the learner's curricula, and the page resolves the matching dungeon (`game.listDungeons()` filtered by `slug`) and loads it via the now-player-aware `GET /game/dungeon/{id}`. Same underlying gap (dungeon selection not wired end-to-end for non-DSA domains) closed by a different, arguably simpler mechanism. **VERIFIED**
- [x] ~~Remove the fake `dsa-dungeon-01` constant and pass the selected UUID end-to-end.~~ **CORRECTED 6 September 2026:** on inspection, `frontend/lib/config.js`'s `DUNGEON_ID`/`DOMAIN` constants were not actually consumed anywhere in the frontend (verified: zero importers of either, including via relative-path imports) -- so there was no live UUID-substitution bug to fix, just two dead exports. Deleted them. **VERIFIED**
- [x] ~~Stop filtering every dungeon through the DSA-only `TOPIC_GRAPH`.~~ **FIXED 9 September 2026:** the client-side `normalizeDungeon()` (`frontend/lib/api/client.js`) filtered every room through the DSA-only `TOPIC_GRAPH`, silently rendering zero rooms for every non-DSA curriculum. Replaced with real per-player room status computed server-side (`backend/routes/game.py::_annotate_rooms_for_player`), generalized to any curriculum via `services/curricula.py`'s own prerequisite graph. **VERIFIED**
- [x] ~~Add generic/domain-specific room labels and visuals for non-DSA topics.~~ **FIXED 9 September 2026:** room labels now come from the real competency data (`GET /learning/pathway`'s `competencies[].label`) per curriculum, not a DSA-only lookup table. Monster sprite art stays DSA-specific (no new art assets), reused deterministically per-topic for non-DSA rooms as acknowledged cosmetic variety, not a data claim. **VERIFIED**
- [x] ~~Fix internal recommendation links so they target the selected dungeon/competency, not `/dungeon#...` without a resolvable dungeon.~~ **FIXED 9 September 2026:** `/dungeon` now has a real "Recommended Order" panel driven by the prerequisite-topologically-sorted `pathway[]` array (same engine as `/stats`), with each item selecting the real corresponding map node. **VERIFIED**
- [x] ~~Give the professional Academy/Admin shell its own plain, non-game visual identity, distinct from Quest mode's pixel-art/dungeon skin.~~ **DONE 9 September 2026:** `AcademyHub.jsx` already used the plain component set (per the "Default landing / professional theme" row above); `/dungeon` and `/admin` did not, and have now been rebuilt on it too. `Pixel*` styling is now confined to Quest's own opt-in routes (`/combat`, `/boss`, `/character`, `/leaderboard`) -- see PR #39. **VERIFIED**
- [ ] Add an end-to-end browser test covering Academy → each of four domains → room renders → one answer submits → progress returns.
- [ ] Add browser tests for refresh, back navigation, double submit, missing API, empty data and a second learner.
- [ ] Finish a clean frontend production build in CI; do not use old `.next` output as evidence.

### 3.3 Make one problem-statement vertical slice undeniable

- [ ] Choose **Official Statistics** as the canonical demo domain; keep DSA as an engineering sample, not the pitch centre.
- [ ] Define one real role persona and target matrix using sourceable MoSPI/CBC material; label any team-authored target as `PROVISIONAL`.
- [ ] Complete: profile → diagnostic → competency evidence → gap explanation → ordered pathway → provider-labelled recommendation → practice → re-assessment → admin change.
- [ ] Make every result expose `why`, `evidence`, target version, formula version and timestamp.
- [ ] Correct admin gap counts to count distinct learners from their latest relevant assessment.
- [x] ~~Replace "weekly leaderboard" copy or implement a real weekly time window.~~ **FIXED 6 September 2026:** changed the heading to "ALL-TIME RANKS"/"सर्वकालिक रैंक", matching what the backend actually ranks by (`total_xp`, lifetime) -- no real weekly window exists to implement. `/leaderboard` is also now gated behind the `preferred_mode` Quest-mode toggle alongside the rest of the Quest routes, since it's `Pixel*`-styled XP/streak content, not part of the professional path. **VERIFIED**
- [x] ~~Either complete the guild raid interaction or remove it from the official demo path.~~ **PARTIALLY RESOLVED 6 September 2026:** took the "remove from the official path" branch, not "complete it" -- `/guild` (and the rest of Quest mode) is now gated behind the opt-in `preferred_mode` toggle, off by default, so it is no longer on the professional/official demo path at all. The raid interaction itself is still not complete: `frontend/lib/api/client.js`'s `joinGuildRaid()` (a real, correctly-wired call to `/game/guild/raid/join` and `/game/guild/raid/status`) has zero callers anywhere in the frontend -- `/guild`'s current UI (`AdaptivePracticeDsaQuest`) is a self-contained WASM code-arena mockup with local-only state, entirely disconnected from that real backend raid system. Left as-is rather than deleted (unlike the dead `dsa-dungeon-01` constant above, this is real, working backend-integration code for a feature that's legitimately still future scope, not a fake value) -- completing it is still open if Quest mode is ever built out further. **VERIFIED** — **UPDATE 9 September 2026:** the fake `AdaptivePracticeDsaQuest` mockup this entry describes no longer exists; `/guild` was deleted and repurposed to `/dsa-sandbox`, a real feature (see the Guild/leaderboard row in section 1). `joinGuildRaid()`'s zero-callers status, described above, is otherwise unchanged and still open.

### P0 exit gate

- [ ] One clean checkout starts from documented commands.
- [ ] Backend tests, frontend lint, frontend build and cross-domain browser smoke all pass in CI.
- [ ] The three-minute demo succeeds offline five consecutive times after a data reset.
- [ ] Every slide claim maps to a working screen, test, source, or explicitly labelled roadmap item.

## 4. P1 — trustworthy differentiation

### 4.1 Competency model

- [ ] Model `framework_version`, `role`, `activity`, `competency`, `target_level`, `source`, `approved_by`, `valid_from` and `valid_to`. **PARTIAL:** `models/governance.py::RoleTarget` has all eight fields except `activity` -- FRAC is role→activity→competency, this table jumps role→competency directly, a documented gap in `services/role_targets.py`'s own docstring, not silently missing. Adding `activity` is a schema change against real learner data going forward, not done here.
- [x] ~~Use designation/job role/department/assignment to choose an explicit versioned role target; stop calling experience-cap targeting "role-aware."~~ **DONE 10 September 2026:** see the "Competency targeting" row in section 1 above -- the resolver and precedence policy were already fully correct; the DB table backing it had simply never been seeded, so it was unreachable through any real route until now. **VERIFIED**
- [x] ~~Separate self-report, diagnostic, observed practice, reviewer evidence and imported provider evidence.~~ **ALREADY DONE, verified 10 September 2026 (not previously checked off despite being real):** `models/governance.py::EvidenceRecord.evidence_type` is a DB `CHECK` constraint over exactly these five values; `services/learning_engine.py`'s `SCORING_EVIDENCE_TYPES = ("observed_practice", "self_report")` and `UNSCORED_EVIDENCE_TYPES = ("reviewer", "diagnostic", "provider_imported")` keep only the first two moving the score, with the other three recorded and reported (`recorded_unscored_evidence_types`) but never blended in. `tests/test_competency_evidence_types.py` (11 tests) covers this. **VERIFIED**
- [x] ~~Display evidence coverage and uncertainty; "no evidence" must never become "low ability."~~ **ALREADY DONE, verified 10 September 2026 (not previously checked off despite being real):** `learning_engine.py`'s `NO_EVIDENCE = "NO EVIDENCE"` constant and confidence tiering keep an unassessed competency visibly distinct from a low-scoring one; `tests/test_competency_evidence_coverage.py` (7 tests, explicitly citing "CLAUDE.md invariant #3") is the regression guard. **VERIFIED**
- [ ] Add human override with reason, actor and immutable audit event.
- [ ] Map the current curriculum to KCM/FRAC only where an official mapping can be cited; do not imply CBC approval.
- [ ] Seek MoSPI/NSSTA/CBC/domain-expert validation. **BLOCKED-EXTERNAL**

### 4.2 Grounded content and quiz governance

- [ ] Create immutable source, source-version and chunk records with page/section locators and hashes.
- [ ] Add provenance-preserving PPTX and video/audio-transcript ingestion for `PS-11`; retain slide/timecode locators and bounded media-processing jobs.
- [ ] Implement parser → semantic chunk → embedding → access-filtered retrieval → generation → verification.
- [ ] Show page/section citations and source preview for every generated item.
- [ ] Add abstention when retrieved evidence is insufficient or contradictory.
- [ ] Treat uploaded/retrieved text as untrusted data; defend against embedded prompt instructions.
- [ ] Add item lifecycle: `draft → auto_checked → expert_review → approved → pilot → published → retired`.
- [ ] Auto-check one defensible answer, unique options, answer leakage, unsupported numbers, duplicates and unsafe content.
- [ ] Require an authorized reviewer before generated items enter scored assessments.
- [ ] Build a gold set with domain-expert labels; measure citation correctness, groundedness, answer validity, Recall@K and cross-tenant leakage.
- [ ] Version prompts, models, retrieval configuration and evaluation results.
- [ ] Add a bounded learner assistant for `PS-06`: answer only from authorized cited sources, expose limitations, abstain/escalate outside scope, and test prompt injection/data leakage.

### 4.3 Integration that is honest and replaceable

- [x] ~~Define `LearningProviderAdapter` for catalogue search, course details, enrolment request, completion import, health and reconciliation.~~ **ALREADY DONE, verified 10 September 2026 (not previously checked off despite being real):** `integrations/provider.py::LearningProviderAdapter` is a `typing.Protocol` with exactly these six methods. **VERIFIED**
- [x] ~~Implement a deterministic `SimulatedIGOTAdapter` with conspicuous simulated badges, fixed fixtures and contract tests.~~ **ALREADY DONE, verified 10 September 2026 (not previously checked off despite being real):** `SimulatedIGOTAdapter.status = "SIMULATED"`, every method returns a fixed, honestly-negative fixture (`found=False`, `accepted=False, reason="simulation-only"`, empty lists), contract-tested by `tests/test_api_integration_lane5.py` including `test_provider_failure_outcomes_do_not_look_like_live_records`. **VERIFIED**
- [x] ~~Rename current "configured" status: an environment variable alone must not prove integration; require a successful authenticated health/capability check.~~ **DONE 10 September 2026:** see the "Recommendations" row in section 1 above -- new `LiveHTTPProviderAdapter` makes a real health check and `integration_status()` now reports the documented `LIVE`/`CATALOGUE` vocabulary based on its outcome, not the env var's presence. "Authenticated" is not yet real (the health check is unauthenticated `GET {base_url}/health`, since no approved credential scheme exists to authenticate with -- PS-05/PS-17 remain BLOCKED-EXTERNAL below), so this closes the "don't fabricate LIVE from a variable" half of the item honestly, not the "authenticated" half, which has no real credential to attach yet. **VERIFIED**
- [ ] Keep provider record IDs, timestamps, source environment and raw response hash for imported evidence.
- [ ] Add timeouts, retry with jitter, circuit breaker, idempotency key, sync cursor, dead-letter handling and reconciliation report.
- [ ] Obtain approved iGOT endpoint scope, credentials, sandbox, data-sharing basis and owner. **BLOCKED-EXTERNAL**

### 4.4 Adaptive lab, dashboards and one bilingual path

- [ ] Build one bounded Official Statistics learning lab—prefer data quality, sampling, or AI-assisted statistics—with deterministic expected outputs and a learning explanation (`PS-08`).
- [ ] Do not execute arbitrary learner code on the API host. If code execution becomes necessary, use an isolated short-lived sandbox with CPU/memory/time/network limits.
- [x] ~~Deliver one complete English/Hindi journey: navigation, profile, questions, feedback, errors and citations.~~ **EXPANDED 10 September 2026:** the frontend UI-chrome catalogue (`frontend/lib/i18n/translations.js`, ~259 keys per language) now covers English, Hindi, plus the 9 next-most-spoken languages from the 2011 Census (Bengali, Marathi, Telugu, Tamil, Gujarati, Urdu, Kannada, Odia, Malayalam) -- 11 languages total. `LanguageContext.jsx`/`LanguageSwitcher.jsx` needed zero code changes; both were already fully generic over the `TRANSLATIONS`/`LANGUAGES` data. `services/curricula.py`'s Hindi-only overlay was generalized to a `curricula_<lang>.json` per language (same field selection as the existing file, still excluding literal source citations), and the small template-string dicts in `learning_catalog.py`/`learning_engine.py`/`learning_analytics.py` got the same 9-language treatment with `{label}`/`{types}`/`{version}` placeholders verified intact. The three `lang=` route patterns were widened from `(en|hi)` accordingly. See PR #42. **VERIFIED**
- [ ] Keep internal IDs language-neutral and test mixed-script input, font rendering and screen-reader output.
- [ ] Have translations reviewed by a fluent human; machine translation alone is not acceptance evidence. **STILL OPEN as of PR #42:** every non-English, non-Hindi string (9 languages, both the frontend catalogue and the backend curricula/template strings) was machine-translated -- via `backend/i18n_pipeline/` (Google Translate's public endpoint, MyMemory as fallback), never hand-translated by the assistant, per explicit instruction not to spend assistant tokens on manual translation. That pipeline is documented and re-runnable, but its output is a first pass exactly like the existing `curricula_hi.json`: this item stays unchecked until a fluent human reviews the wording for each language, same standard already applied to Hindi.
- [ ] Add honest learner-dashboard learning hours: derive them from real internal/provider events or show `not available`; never invent time (`PS-13`).
- [ ] Limit the demo admin dashboard to latest-distinct-learner aggregates and descriptive trends. Defer predictive workforce claims until representative data, baselines and validation exist (`PS-14`).

## 5. P2 — controlled-pilot engineering

### 5.1 Identity, privacy and authorization

- [x] Implement and live-test a local standards-based OIDC resource-server verifier against
  Keycloak, including issuer/audience/signature/time validation and fail-closed JWKS behavior.
  This is development evidence, not an approved production/government IdP. **VERIFIED**
- [ ] Integrate the approved production identity provider, browser Authorization Code + PKCE
  session/logout flow, and accountable key-rotation/outage operations. **BLOCKED-EXTERNAL/SHARED**
- [x] Implement the Lane 2 server-side matrix recognizing learner, trainer, content reviewer,
  department admin, organization admin and auditor; add issuer/sub identity binding, first-admin
  bootstrap, learner-own-record scope and deployment-tenant guards. Trainer/cohort and department
  object scope remain absent/fail-closed until authoritative relationships exist. **VERIFIED**
- [ ] Attach the Lane 2 identity/RBAC boundary to every protected product route with 401/403 and
  negative API tests; this is Lane 5-owned integration work.
- [ ] Add organization/tenant scope to every personal/content/evidence query and negative authorization tests.
- [x] Create the current subject-data inventory plus transactional internal export/deletion
  primitives, retention classification/policy guard and audited execution boundary. **VERIFIED**
- [ ] Approve lawful purpose, notice/consent where applicable, minimization, retention durations,
  correction/subject-rights HTTP workflow and processor register; automate expiry only after the
  accountable privacy/legal owner approves the policy. **BLOCKED-EXTERNAL/SHARED**
- [ ] Never expose model prompts, API keys, tokens or learner PII in client code, logs, analytics or screenshots.
- [x] Implement a bounded, versioned AES-256-GCM envelope/key-rotation primitive and explicit
  adoption contract for a future reviewed sensitive field. No current model uses it. **VERIFIED**
- [ ] Configure production TLS/storage/backup encryption and approved KMS/HSM custody, access,
  rotation, recovery and compromise procedures. **BLOCKED-EXTERNAL/OPERATIONAL**
- [x] Provide an append-only audit model/write path and atomic events for identity bootstrap,
  binding lifecycle and internal export/deletion operations. The append-only guarantee itself is
  an application-layer property (`security.data_rights`/`RETENTION_CLASSIFICATION` never delete
  `audit_events` on a subject request), never a database one. On PostgreSQL, `audit_events` is
  additionally protected against in-place mutation by a database-level trigger rejecting `UPDATE`
  only -- named precisely as that, not "append-only", because `DELETE` is intentionally still
  possible through `scripts/retention_job.py` under a cited maximum (Package V, migration
  `4631f204d4ba`, corrected a real defect where Package U's original trigger rejected `DELETE` too
  and would have made the retention job unusable for its only registered category). This is a
  bug-catching safety net for the app's own connection role, not a security boundary against
  someone holding those same credentials, who can disable it. **VERIFIED**
- [ ] Integrate audited privileged route reads/writes, IdP role reconciliation, content approval
  and model decisions across their owning lanes.
- [ ] Track the staged DPDP commencement accurately: the Gazette notifications phase most Data Fiduciary duties in on 14 May 2027 (with Rule 4 on 14 November 2026). Build to the final Rules now, but do not call every duty legally operative on 29 August 2026.
- [ ] Obtain legal review of the operating entity and transitional IT Act section 43A/SPDI applicability before processing real personnel data. **BLOCKED-EXTERNAL**

### 5.2 Persistence and API contracts

- [x] Add PostgreSQL and Alembic migrations with migration-gated PostgreSQL startup while retaining
  SQLite as the documented zero-setup local-demo profile. **VERIFIED**
- [ ] Add uniqueness, foreign keys, tenant keys, version fields and indexes based on measured queries.
- [ ] Publish versioned OpenAPI and typed frontend client; add schema compatibility checks in CI.
- [ ] Add pagination, idempotency and consistent error envelopes.
- [ ] Queue document processing and AI work outside request threads; expose job state and cancellation.
- [x] Complete local PostgreSQL backup/restore plus forward/backward migration drills, including
  adversarial restore-copy cleanup and concurrent temporary-path evidence. This does not imply a
  scheduled, encrypted, offsite production DR capability. **VERIFIED**

### 5.3 Security, accessibility and reliability

- [ ] Use OWASP ASVS 5.0 as the web verification baseline and add dependency, secret, SAST and DAST scans.
- [ ] Threat-model upload parsing, prompt injection, cross-tenant retrieval, evaluator manipulation, broken access control and provider callbacks.
- [ ] Add bounded rate limits, malware/file-type validation, egress allow-listing, CSP/security headers and TLS.
- [ ] Treat applicable GIGW 3.0 `MUST` checkpoints and notified IS 17802 requirements as government acceptance controls; retain WCAG 2.1 AA evidence and use WCAG 2.2 AA as the forward-compatible engineering target.
- [ ] Test keyboard-only operation, visible focus, zoom/reflow, contrast, reduced motion, screen readers and accessible errors manually as well as automatically.
- [ ] Add structured redacted logs, request IDs, metrics, traces, health/readiness checks, alert owners and incident runbooks.
- [ ] Define and load-test measurable SLOs before claiming scale.
- [ ] For a government deployment, establish the CERT-In point of contact, synchronized clocks, six-hour report workflow for specified incidents, and an India-resident copy of required ICT logs retained for a rolling 180 days. Confirm exact ownership with MoSPI/CERT-In. **BLOCKED-EXTERNAL/LEGAL**

## 6. P3 — production authorization, not a hackathon checkbox

- [ ] Independent application-security assessment and remediation sign-off.
- [ ] STQC/GIGW assessment where required by the sponsoring organization.
- [ ] MoSPI/CBC/NSSTA competency and content governance ownership.
- [ ] iGOT production contract, credentials, security review and reconciliation owner.
- [ ] DPDP roles, notices, grievance contact, processor agreements and retention/deletion operation approved.
- [ ] Disaster-recovery exercise, capacity test, operational on-call and cost budget accepted.
- [ ] Real pilot with representative officials; accessibility, fairness, usefulness and learning-outcome results reviewed.
- [ ] Formal go-live decision by the accountable government/product/security/privacy owners.

Passing P0 makes a credible hackathon prototype. Passing P1 makes a strong demonstrator. Passing P2 makes a controlled-pilot candidate. Only P3 can support a production-ready claim.

## 7. Authoritative source register

| Claim used in this checklist | Primary source |
|---|---|
| SIH 2024 team formation and official idea-selection criteria | [Smart India Hackathon 2024 College SPOC Guidelines](https://www.sih.gov.in/letters/Guidelines-College-SPOC.pdf) |
| FRAC links positions to roles, activities and competencies | [CBC, Mission Karmayogi: A Silent Revolution](https://cbc.gov.in/sites/default/files/2026-01/Mission-Karmayogi-A-silent-revolution-May-2023.pdf) |
| Current KCM direction and iGOT competency mapping | [Capacity Building Commission: Karmayogi Competency Model](https://www.cbc.gov.in/karmayogi-competency-model-kcm) |
| TPAC reviews and approves NSSTA training calendar, syllabi, duration and method | [MoSPI Annual Report 2023–24](https://www.mospi.gov.in/sites/default/files/publication_reports/AnnualReport_2023-24.pdf) |
| Authenticated KB-iGOT interfaces exist; tokens/environment access are required | [KB-iGOT integration contract](https://github.com/KB-iGOT/deterministic-chatbot/blob/main/docs/INTEGRATION_CONTRACT.md) |
| DPDP Rules 2025 and enforcement material | [MeitY: Digital Personal Data Protection Rules 2025](https://www.meity.gov.in/documents/act-and-policies/digital-personal-data-protection-rules-2025-gDOxUjMtQWa) |
| Staged commencement of DPDP Act duties | [MeitY: Enforcement Timeline for the DPDP Act](https://www.meity.gov.in/static/uploads/2025/11/c56ceae6c383460ca69577428d36828b.pdf) |
| Government web/app quality and WCAG 2.1 AA baseline | [GIGW 3.0 new features](https://guidelines.india.gov.in/new-features-of-gigw-3-0/) |
| Government accessibility applicability and mandatory checkpoints | [GIGW 3.0 scope and objective](https://guidelines.india.gov.in/scope-and-objective/) |
| Current web application verification baseline | [OWASP ASVS 5.0](https://owasp.org/www-project-application-security-verification-standard/) |
| Generative-AI risk lifecycle guidance | [NIST AI RMF Generative AI Profile](https://www.nist.gov/publications/artificial-intelligence-risk-management-framework-generative-artificial-intelligence) |
| Current general accessibility recommendation | [W3C WCAG 2.2](https://www.w3.org/TR/WCAG22/) |
| CERT-In government ICT log direction | [CERT-In Directions under section 70B](https://cert-in.org.in/PDF/CERT-In_Directions_70B_28.04.2022.pdf) |

## 8. Evidence log

| Date | Evidence | Result | Owner |
|---|---|---|---|
| 2026-08-29 | `python -m pytest backend/tests` | 42 passed | Repository audit |
| 2026-08-29 | frontend lint | passed | Repository audit |
| 2026-08-29 | route/config/client inspection | three non-DSA Quest paths blocked in browser | Repository audit |
| 2026-08-29 | 174 KB pasted planning source reconciled with repository and primary sources | useful proposals retained; unsupported claims removed | Research pass |
| 2026-09-01 | `backend/.venv/Scripts/python.exe -m pytest -q` | 237 passed; 2 pytest-cache permission warnings | Lane 2 Packages A–N closure |
| 2026-09-01 | PostgreSQL 16 Alembic forward/backward/startup drills | baseline and follow-up migrations, stale-revision refusal and empty/head startup verified | Lane 2 + reciprocal review |
| 2026-09-01 | Local Keycloak 26.7.2 OIDC verification and RBAC/bootstrap negatives | verifier/binding/policy foundation accepted; product routes explicitly still unprotected | Lane 2 + reciprocal review |
| 2026-09-01 | Concurrent PostgreSQL backup/restore and adversarial regression contract | accepted; no container temp residue in recorded live drill | Lane 2 + reciprocal review |
| 2026-09-01 | Full backend gate after immutable Package P and reviewed Package Q | 272 passed; 2 pytest-cache permission warnings; Package P review findings remain open | Lane 2 closure pass |
| 2026-09-01 | Live PostgreSQL 4-worker concurrency drill (pre-fix, Codex-run) | `deleted_sets=[set(), set(), {'1','2','3'}, set()]`; 3 of 11 expired rows deleted, 8 abandoned; 2 young rows correctly untouched | Confirmed real defect: unlocked candidate SELECT, reproduced by Codex; not overwritten by the fix below |
| 2026-09-01 | Full backend gate after Package P/S (atomic PostgreSQL row-claiming fix) | 339 passed; 2 pytest-cache permission warnings; 0 failures | Claude Code, live-tested, awaiting Codex final review |
| 2026-09-01 | Live PostgreSQL 4-worker concurrency drill (post-fix, same scenario) | 11 expired + 2 young rows; per-worker deletions pairwise-disjoint; union = all 11 expired IDs; deleted-count sum = 11; durable audit deleted-count sum = 11; young rows untouched; clean `0/0` final rerun, zero misleading audit events | Claude Code; exact opposite result to the pre-fix row above under the identical scenario |
| 2026-09-01 | Full independent Lane 2 audit (security/rbac.py, security/data_rights.py, security/identity_bootstrap.py, models/identity.py, security/audit.py, migrations re-read fresh) plus full backend gate | 341 passed, 0 failures; exact warning count/type varies by run (2 SQLite datetime-adapter deprecations on this run; Codex separately observed 4, including 2 `.pytest_cache` warnings, on a concurrent run — both accurate for their own run, not a code discrepancy) | Claude Code (Codex ran out of session credits mid-review); fixed `delete_subject_data()`'s stale pre-delete-snapshot `deleted_counts` (now real DELETE rowcounts, live-verified against PostgreSQL) and `BoundPrincipal.audit_actor`'s non-injective `\|`-joined encoding (now canonical JSON, matching the identical fix already applied to `identity_bootstrap.py`) |
| 2026-09-01 | Second external-audit review + live PostgreSQL `audit_events` append-only trigger drill | 3 of 4 claimed gaps (RLS, full audit triggers w/ actor context, evidence SHA-256 self-hashing, legacy ETL) rejected with technical reasoning in `docs/internal/LANE2_SYNC.md`; 1 correctly-scoped item implemented and live-verified: normal insert succeeds, direct UPDATE/DELETE against `audit_events` rejected by the database with the exact expected message, row survives unmodified, owner-role `DISABLE TRIGGER`/`ENABLE TRIGGER` bypass-and-restore confirmed genuine, downgrade/upgrade both clean; 341 passed | Claude Code; migration `036de46dd515`, SQLite-side no-op confirmed via the existing migration-chain regression suite |
| 2026-09-01 | Codex cold immutable audit of Packages S/T/U at `1f0c576` | S and T ACCEPTED (independent re-drill and rowcount/encoding checks passed); U's migration mechanics verified but its INTEGRATION verdict REJECTED: a synthetic 30-day maximum + the accepted retention job reached the DELETE path and failed with `ProgrammingError`/`RaiseException: ... DELETE is not permitted`, `ROW_REMAINS=1` — audit_events is the retention job's only registered category, so U's unconditional DELETE rejection made the job permanently unusable for it | Codex; full audit and Package V handoff spec in `docs/internal/LANE2_SYNC.md` (~line 2420) |
| 2026-09-02 | Package V fix + full backend gate + live PostgreSQL integration contract | New migration `4631f204d4ba` retires only the DELETE rejection (UPDATE stays rejected, named precisely as not "append-only"); a new committed opt-in integration test file proved at the real head: UPDATE rejected/DELETE permitted, a synthetic maximum can delete `audit_events` through the retention job, the exact 4-worker/11-expired/2-young drill is still exact against the real trigger-protected table, and downgrade/upgrade across the new revision is clean — all 4 passed in 6.71s, disposable database confirmed dropped after; full gate 341 passed with PostgreSQL stopped (proving the opt-in file skips, not fails, without Docker), 345 passed with it running; `alembic check` clean at head; `git diff --check` clean | Claude Code; every prior "database-enforced append-only" claim in this file, `README.md`, `CLAUDE.md`, `CODEX.md` and `docs/internal/SIH26101_TEAM_ORCHESTRATION.md` corrected to the real UPDATE-only boundary; awaiting Codex review |
| 2026-09-02 | Codex immutable review of Package V at `847c0a8` | 4/4 integration tests passed (5.87s); Codex independently reproduced the original Package U P1 failure closing correctly at head (`DELETED_COUNT=1`, `AUDIT_DELETED_COUNT=1`); full gate 345 passed, 4 warnings, 0 failures (99.14s); `alembic check` clean; no leaked `sih_pkgv_%` databases. **Production migration/retention behavior ACCEPTED outright.** Two bounded P2 findings raised (not blocking): the 4-worker regression only synchronized at thread-pool submission (a serial run would also pass, so it did not itself prove the row-claim race stays closed) and the disposable-database fixture leaked on a pre-yield failure; several current-status doc claims were also stale versus corrected paragraphs elsewhere in the same files, and the ETL rationale still cited "no tenant model" in places instead of the accepted no-real-source-dataset reasoning | Codex; full review and exact closure assignment in `docs/internal/LANE2_SYNC.md` |
| 2026-09-02 | Package V hardening pass closing both Codex P2 findings | Cleanup: `_disposable_postgres_database()` wraps create/migrate/yield in unconditional `try/finally`; new deterministic regression injects a post-create/pre-yield failure and confirms via direct `pg_database` query that the generated database does not survive. Forced overlap: a test-only `_BarrierSyncSession` (zero production-file changes) blocks all 4 workers at a shared barrier immediately after their `FOR UPDATE SKIP LOCKED` candidate select returns, while row locks are still held; a new negative control runs an equivalent unlocked-select flow under the identical barrier and deterministically fails the same contract (only 3 of 11 rows ever deleted, exact IDs asserted) — a separate uncommitted sanity script confirmed the same broken logic passes when run serially (no forced overlap), proving the barrier does real synchronization work, not decoration. Final-rerun audit-absence now explicitly queried, not inferred. All 6 tests pass in 8.65s; full gate 341 passed/6 skipped with PostgreSQL stopped, 347 passed with it running; `alembic current` → `4631f204d4ba (head)`; `alembic check` clean; `git diff --check` clean; no leaked databases | Claude Code; every stale current-status claim and the ETL rationale corrected across `README.md`, `CLAUDE.md`, `CODEX.md`, `docs/internal/SIH26101_TEAM_ORCHESTRATION.md`, this file; awaiting Codex's narrow re-review of forced-contention validity, pre-yield cleanup, final-rerun audit-event absence, and current-doc consistency |
| 2026-09-02 | Codex final narrow immutable review of Package V hardening at `ac5a2e7` | Scope verified: one PostgreSQL integration-test file plus truth/evidence docs; no production retention/migration change. Five consecutive real-PostgreSQL focused runs passed (**30/30 checks** across 5 × 6 tests); fresh full gate **347 passed, 4 warnings, 0 failures in 52.88s**; no `sih_pkgv_%` database remained; Alembic at `4631f204d4ba (head)` with no new operations; compile-all/diff checks passed; a fresh real Keycloak learner token verified through `OIDCVerifier`. Forced overlap, unlocked negative control, pre-yield cleanup and explicit final-rerun audit absence are all valid. **Package V and O-C ACCEPTED; no remaining local Lane 2 correctness finding.** | Codex; no claim of remote CI, protected product routes, organization-row tenancy, approved production IdP, KMS/DR, compliance or whole-product production readiness |
| 2026-09-02 | `main` merged into lane-2; two-mode data scaffold (`players.preferred_mode`) implemented; Lane 2 coverage-hardening pass | Merge: one conflict (stale local branch ref, not real content), resolved by fast-forwarding first; 351 passed post-merge, no leftover conflict markers. Scaffold: migration `640603a37f2f` adds `players.preferred_mode` (default `"professional"`) with a `CHECK` constraint applied via Alembic batch mode (works on SQLite and PostgreSQL alike, unlike the trigger-precedent dialect gate, since an added column is part of SQLAlchemy's comparable metadata); live-verified on real PostgreSQL (constraint rejects an invalid INSERT, downgrade/re-upgrade clean, `alembic check` clean) and on a real Alembic-migrated SQLite file (constraint genuinely enforced there too, not just documented). One real bug caught pre-commit: the model needed `server_default=`, not just `default=`, or `alembic check` reported a spurious drift. Coverage: installed `pytest-cov` (now in `requirements-dev.txt`); Lane 2-owned code 84% → 94%; `db/database.py` 74% → 100%; `db/seed.py`, `schemas/accuracy.py`, `schemas/learning.py`, `schemas/question.py` 0% → 100%/78%. Full gate: **402 passed** with PostgreSQL stopped (6 skipped), **408 passed** with it running; `alembic check` clean at head `640603a37f2f`; `git diff --check` clean | Claude Code; new files `backend/models/enums.py`, `backend/migrations/versions/640603a37f2f_add_players_preferred_mode.py`, `backend/tests/test_core_learning_mode.py`, `backend/tests/test_core_schemas.py`, `backend/tests/test_core_learning_schemas.py`, `backend/tests/test_core_seed.py`; `docs/contracts/data-authorization.md` section 8 (new); full detail in `docs/internal/LANE2_SYNC.md` |

| 2026-09-03 | Package W cross-lane usability and reciprocal review | W-A deterministic repository facade accepted by Claude at `3a75b28`; the suggested role/competency isolation regression closed at `be9e338`. W-B added the privacy-safe status command and per-lane integration guide. Codex reproduced and drove closure of fresh/missing-table and legacy-column schema crashes; W-C `8d0d1de` uses table-level `COUNT(*)` and adds an adversarial legacy-schema contract. Full gates: **442 passed, 6 skipped** with PostgreSQL stopped; **448 passed** with local PostgreSQL healthy. Live status and Alembic both reported `640603a37f2f (head)`, zero missing tables and no new operations. W-C awaits Claude's final immutable review; no protected-route, hosted-DB, organization-tenancy or production claim is made | Codex + Claude Code; full review trail in `docs/internal/LANE2_SYNC.md`, consumer instructions in `docs/internal/LANE2_INTEGRATION_GUIDE.md` |

Append future entries; never overwrite failed evidence with a later success.
