"""Shared pytest fixtures for backend/tests — owned by Lane 6 (Quality, Security, Release &
Evidence; see SIH26101_TEAM_ORCHESTRATION.md section 2). Add cross-lane fixtures here;
lane-specific fixtures belong in that lane's own test_<lane>_*.py file instead.

Empty scaffold — no shared fixtures exist yet. The three flat legacy test files
(test_progression.py, test_combat_model.py, test_learning_platform.py) predate the per-lane
filename convention (test_core_*.py, test_competency_*.py, test_content_ai_*.py,
test_api_integration_*.py, test_release_*.py) and remain read-only regression baselines unless
Lane 6 explicitly reassigns one (SIH26101_TEAM_ORCHESTRATION.md section 2).
"""
